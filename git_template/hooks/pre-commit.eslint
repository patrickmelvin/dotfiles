if [ $SKIP_LINT ]; then
  exit 0;
fi

if [ ! -f ./`git rev-parse --show-cdup`/ui/node_modules/.bin/eslint ]; then
  echo 'Cannot find eslint binary. Skipping check...';
  exit 0;
fi

git diff --name-only --cached | grep '\.js' | xargs ./`git rev-parse --show-cdup`/ui/node_modules/.bin/eslint
